# terraform.tfvars.example
# Copy to terraform.tfvars and fill in your values.
# terraform.tfvars is gitignored — do not commit it.

proxy_address = "yourcluster.teleport.sh"
region        = "us-east-2"
user          = "you@example.com"
env           = "dev"
team          = "dev"

node_count    = 2
instance_type = "t3.micro"

# OS login created on each agentless host.
# Must match a login in your dev-access or platform-dev-access role.
ssh_login = "teleport-user"
