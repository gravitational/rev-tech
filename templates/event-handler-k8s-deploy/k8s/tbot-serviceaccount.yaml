apiVersion: v1
kind: ServiceAccount
metadata:
  name: tbot
# tbot needs rest.InClusterConfig() to work — it uses the default SA token path
# (/var/run/secrets/kubernetes.io/serviceaccount/token) to call the Kubernetes
# API for the kubernetes_secret destination type.
automountServiceAccountToken: true
