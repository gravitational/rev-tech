# Generated by scripts/render-dashboards.py. Hand edits are lost on the next
# render -- change the source in dashboards/ instead.
rendered/
chart/dashboards/

# Cross-compiled exporter binaries (~49 MB each). Rebuild with:
#   cd exporter && make build   (or see exporter/deploy/README.md)
exporter/teleport-usage
exporter/teleport-usage.exe
exporter/teleport-usage-linux-*
exporter/teleport-mau-tracker
exporter/teleport-tpr-tracker

# Container image tarballs produced by `docker save` when loading an image into
# a cluster without a registry.
*.tar

# Python
__pycache__/
*.pyc
.venv/

# Editor / OS
.DS_Store
